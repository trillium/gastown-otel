services:
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.136.0
    container_name: gt-victoriametrics
    ports:
      - "127.0.0.1:8428:8428"
    volumes:
      - vm-data:/storage
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=30d"
      - "--opentelemetry.usePrometheusNaming=true"
    restart: unless-stopped

  victorialogs:
    image: victoriametrics/victoria-logs:v1.45.0
    container_name: gt-victorialogs
    ports:
      - "127.0.0.1:9428:9428"
    volumes:
      - vl-data:/vlogs-storage
    command:
      - "--storageDataPath=/vlogs-storage"
      - "--retentionPeriod=30d"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:12.3.3
    container_name: gt-grafana
    ports:
      - "127.0.0.1:9429:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin  # dev-only stack — change before any shared/remote deployment
      - GF_PLUGINS_PREINSTALL=victoriametrics-logs-datasource
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    restart: unless-stopped
    depends_on:
      - victoriametrics
      - victorialogs

volumes:
  vm-data:
  vl-data:
  grafana-data:
